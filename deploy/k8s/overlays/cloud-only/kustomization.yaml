apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: pia
resources:
  - ../../base
patchesStrategicMerge:
  - pia-config-patch.yaml
# No vllm Deployment — set ANTHROPIC_API_KEY (or OPENAI_*) in pia-secrets
