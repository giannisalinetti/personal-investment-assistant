# Deprecated — use watchlists/{stock,etf,etc}.yaml instead.
# Kept temporarily so older docs/scripts that mention this path still find a file.
# PIA loads watchlists/ preferentially; this file is ignored when watchlists/ exists.

stocks: []
etfs: []
